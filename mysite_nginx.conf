# mysite_nginx.conf

# the upstream component nginx needs to connect to
upstream django {
    server unix:///home/admin/SupplierManagementSystem/mysite.sock; # for a file socket
}

    # configuration of the server
server {
    # the port your site will be served on
    listen      8000;
    listen      [::]:8000;
    
   server_name srvsms sms.pmdm.de; # substitute your machine's IP address or FQDN
   return 301 https://$server_name$request_uri;
}    
server {
    # the port your site will be served on
   listen      80;
    listen      [::]:80;
    
   server_name srvsms sms.pmdm.de; # substitute your machine's IP address or FQDN
   return 301 https://$server_name$request_uri;
}    
server {
    	# SSL configuration
	#
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
	#
	# Note: You should disable gzip for SSL traffic.
	# See: https://bugs.debian.org/773332
	#
	# Read up on ssl_ciphers to ensure a secure configuration.
	# See: https://bugs.debian.org/765782
	#
	# Self signed certs generated by the ssl-cert package
	# Don't use them in a production server!
	#
	# include snippets/snakeoil.conf;



    # the domain name it will serve for
    server_name sms.pmdm.de; # substitute your machine's IP address or FQDN
    charset     utf-8;

    ssl_certificate /etc/nginx/ssl/STAR_pmdm_de.crt; # fuegt Zertifikat hinzu
    ssl_certificate_key /etc/nginx/ssl/STAR_pmdm_de.key; # fuegt privaten Schluessel hinzu
    ssl_protocols       TLSv1 TLSv1.1 TLSv1.2; # definiert die Protokolle
    ssl_ciphers         HIGH:!aNULL:!MD5; # definiert die Ciphersuites




    # max upload size
    client_max_body_size 75M;   # adjust to taste

    # Django media
    location /media  {
#    location /SMS/media  {
#        root /home/admin/SupplierManagementSystem/media;  # your Django project's media files - amend as required
         alias /home/admin/SupplierManagementSystem/media;  # your Django project's media files - amend as required
    }

    location /static {
        alias /home/admin/SupplierManagementSystem/static; # your Django project's static files - amend as required Use collectstatic!
    }

    # Finally, send all non-media requests to the Django server.
    location / {
        uwsgi_pass  django;
        include     /home/admin/SupplierManagementSystem/mysite/uwsgi_params; # the uwsgi_params file you installed
    }
}
